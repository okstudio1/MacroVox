{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Default window capabilities for MacroVox dictation + settings windows. Required-permission rationale: shell:allow-open backs Stripe checkout + billing portal links (URLs are hostname-allowlisted on the call sites per SECURITY_AUDIT_2026-04-16 § H9). process:allow-restart backs the auto-updater's post-install relaunch via tauri-plugin-updater. updater:default backs the same updater flow's version-check + download. clipboard-manager:allow-write-text writes transcripts to the system clipboard. Everything else is window-chrome control for the custom-decorations HUD; set-size, inner-size and scale-factor let the HUD widen itself when the Recordings side panel opens.",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-close",
    "core:window:allow-set-size",
    "core:window:allow-inner-size",
    "core:window:allow-scale-factor",
    "core:window:allow-set-min-size",
    "clipboard-manager:allow-write-text",
    "shell:allow-open",
    "updater:default",
    "process:allow-restart"
  ]
}
